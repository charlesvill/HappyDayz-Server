const { Router } = require('express');
const { addModule } = require('../controllers/moduleController');


const moduleRouter = Router();


moduleRouter.post({
  '/eventid/:pageid/photo',
  
})

module.exports = 

